export const chatsDates = $state<{ current: { date: Date; id: string }[] }>({ current: [] });
export const allEventsDates = $state<{ current: { date: Date; id: string }[] }>({ current: [] });
export const todaysEventsDates = $state<{ current: { date: Date; id: string }[] }>({ current: [] });
export const upcomingEventsDates = $state<{ current: { date: Date; id: string }[] }>({
	current: []
});
export const pastEventsDates = $state<{ current: { date: Date; id: string }[] }>({ current: [] });
